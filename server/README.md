# Clofly - Server

## Prepare

Setup you aws ```credentials``` and ```config``` file under ~/.aws. For deamon, make sure the files are under /root.

## Installation

    $ ./install.sh

## Run (Local Test)

    $ ./run.sh
